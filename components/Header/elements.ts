import styled from "@emotion/styled"

export const Header = styled.div`
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  padding: 20px;
  display: flex;
  justify-content: space-between;
  z-index: 20;
`